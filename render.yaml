services:
  - type: web
    name: onserverhost-billing
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fossbilling-db
          property: connectionString

databases:
  - name: fossbilling-db
    databaseName: fossbilling
    user: fossuser
    plan: free
